{
  "name": "practera-voice-agent-server",
  "version": "1.0.0",
  "private": true,
  "description": "Mints short-lived Retell web-call tokens for the Practera site's voice demo widget. Keeps the Retell API key server-side.",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
